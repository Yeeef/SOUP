program Simple;
const aa = 1; bb = 1.5; flag=true;
type
    real_arr=array[2..10] of real;
var x, y, s: integer; arr1: real_arr;

procedure add(var s: real; x, y: integer; xx,yy,zz: boolean);
var a,b,c: real;
procedure add_add(var kkk: real_arr);
    begin
        kkk[1] := 2 + aa + bb;
    end;
begin

	s := x * x + y * y + aa;
end;

{
procedure subtract(var s: real; x, y: integer);
begin
    s := x * x - y * y;
end;
procedure mul(var s: real; x, y: integer);
const ab = 1;
var arrarr: array[1..10] of real_arr;
begin
    s := x * y;
end;}
procedure hello;
var a,b,c : real_arr;
begin
    writeln(aa, bb);
    read(a[10]);
end;
begin
	x := -4;
	y := aa + 1;
	hello;
	add(s + 1, x, y div (aa * aa), 1, 1, 1);
end.